cmake_minimum_required(VERSION 3.16)
project(lineairdb-server)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Find required packages
find_package(Protobuf REQUIRED)
find_package(PkgConfig REQUIRED)

# LineairDB library path
set(LINEAIRDB_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../third_party/LineairDB")

# DuckDB library path (third_party submodule). The shared library is a release
# build produced inside the submodule; scripts/build.sh builds it when absent.
set(DUCKDB_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../third_party/duckdb")
set(DUCKDB_LIB_DIR "${DUCKDB_ROOT}/build/release/src")

# MySQL charset runtime. libstrings contains the collation descriptors and
# handlers; libmysys contains the loader, registry, and runtime
# initialization. The final libstrings entry resolves libmysys
# back-references with static-link semantics.
set(MYSQL_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../third_party/mysql-server")
set(MYSQL_BUILD_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../build")
set(MYSQL_ARCHIVE_DIR "${MYSQL_BUILD_ROOT}/archive_output_directory")
set(MYSQL_CHARSET_LIBRARIES
    ${MYSQL_ARCHIVE_DIR}/libstrings.a
    ${MYSQL_ARCHIVE_DIR}/libmysys.a
    ${MYSQL_ARCHIVE_DIR}/libstrings.a)
set(MYSQL_INCLUDE_DIRS
    ${MYSQL_ROOT}
    ${MYSQL_ROOT}/include
    ${MYSQL_BUILD_ROOT}/include)

# Include directories
include_directories(${CMAKE_CURRENT_SOURCE_DIR})
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/../proto)
include_directories(${LINEAIRDB_ROOT}/include)
include_directories(${DUCKDB_ROOT}/src/include)

# Generate protobuf files
set(PROTO_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../proto")
set(PROTO_FILE "${PROTO_DIR}/lineairdb.proto")
set(PROTO_SRCS "${CMAKE_CURRENT_BINARY_DIR}/lineairdb.pb.cc")
set(PROTO_HDRS "${CMAKE_CURRENT_BINARY_DIR}/lineairdb.pb.h")

# Custom command to generate protobuf files
add_custom_command(
    OUTPUT ${PROTO_SRCS} ${PROTO_HDRS}
    COMMAND ${Protobuf_PROTOC_EXECUTABLE}
    ARGS --cpp_out=${CMAKE_CURRENT_BINARY_DIR} --proto_path=${PROTO_DIR} ${PROTO_FILE}
    DEPENDS ${PROTO_FILE}
    COMMENT "Generating protobuf files"
)

# Add LineairDB as subdirectory
add_subdirectory(${LINEAIRDB_ROOT} ${CMAKE_CURRENT_BINARY_DIR}/LineairDB)

# Source files
set(SOURCES
    # Core
    main.cc
    lineairdb_server.cc
    lineairdb_server.hh
    
    # Network layer
    network/tcp_server.cc
    network/tcp_server.hh
    network/message_handler.cc
    network/message_handler.hh
    
    # RPC layer
    rpc/lineairdb_rpc.cc
    rpc/lineairdb_rpc.hh
    rpc/database_operations.cc
    rpc/decimal_arithmetic.cc
    rpc/decimal_arithmetic.hh
    rpc/flat_plan_encode.cc
    rpc/flat_plan_encode.hh
    rpc/parallel_scan.cc
    rpc/parallel_scan.hh
    rpc/primary_key_operations.cc
    rpc/primary_key_scan_operations.cc
    rpc/read_plan_executor.cc
    rpc/row_codec.cc
    rpc/row_codec.hh
    rpc/secondary_index_operations.cc
    rpc/secondary_index_scan_operations.cc
    rpc/stateless_operations.cc
    rpc/table_stats.cc
    rpc/transaction_control.cc
    rpc/predicate_evaluator.cc
    rpc/predicate_evaluator.hh
    mysql_charset_runtime.cc
    mysql_charset_runtime.hh
    rpc/duckdb_ast_builder.cc
    rpc/duckdb_ast_builder.hh
    rpc/duckdb_bridge_dispatch.cc
    rpc/duckdb_bridge_executor.cc
    rpc/duckdb_bridge_executor.hh
    
    # Storage layer
    database_manager.cc
    database_manager.hh
    transaction_manager.cc
    transaction_manager.hh

    # Protocol
    protocol/message.hh
)

# The generated sources belong to exactly one target: listing them in two
# executables lets a parallel build run protoc twice over the same files.
add_library(lineairdb-proto STATIC ${PROTO_SRCS})
target_include_directories(lineairdb-proto PUBLIC ${CMAKE_CURRENT_BINARY_DIR})
target_link_libraries(lineairdb-proto PUBLIC ${Protobuf_LIBRARIES})

# Create executable
add_executable(lineairdb-server ${SOURCES})

# Link libraries
target_link_libraries(lineairdb-server
    lineairdb
    lineairdb-proto
    pthread
    ${DUCKDB_LIB_DIR}/libduckdb.so
    ${MYSQL_CHARSET_LIBRARIES}
)

# Additional include directories for generated files
target_include_directories(lineairdb-server PRIVATE
    ${CMAKE_CURRENT_BINARY_DIR}
    ${MYSQL_INCLUDE_DIRS})

# Embed an rpath to the DuckDB shared library. start_server.sh runs the binary
# directly, without LD_LIBRARY_PATH.
set_target_properties(lineairdb-server PROPERTIES
    BUILD_RPATH "${DUCKDB_LIB_DIR}"
    INSTALL_RPATH "${DUCKDB_LIB_DIR}")

# Compiler flags to suppress warnings
target_compile_options(lineairdb-server PRIVATE -O3 -Wno-error -Wno-unused-parameter)

# Control-plane client. Speaks the same protocol; links no storage engine.
add_executable(lineairdb-ctl tools/lineairdb_ctl.cc)
target_link_libraries(lineairdb-ctl lineairdb-proto)
target_compile_options(lineairdb-ctl PRIVATE -O2)

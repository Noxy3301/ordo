[defaults]
# Use the local inventory file by default.
inventory = inventory.ini
# Skip manual host key prompts; accept-new is used in inventory ssh args.
host_key_checking = False
# Enable task profiling to identify slow tasks.
callbacks_enabled = profile_tasks
# Increase the number of parallel forks.
forks = 64